use crate::business::error::Result;
use crate::core::dtp::DataType;
use crate::core::msg::StructuredType;

pub fn convert(data_type: &DataType) -> Result<StructuredType> {
    todo!("implement dtp to msg conversion")
}
